<!DOCTYPE html>
<html lang='en'>
    <head>
        <meta charset='UTF-8'/>
        <meta name='viewport' content='width=device-width' initial-scale='1.0' maximum-scale='1.0'/>
        <title>Plantr</title>
        <link rel="shortcut icon" href="/images/plantrLogo.svg" type="image/x-icon">
        <link rel='stylesheet' href='/styles/styles.css'/>
        <link href="https://fonts.googleapis.com/css2?family=Comfortaa:wght@400;700&display=swap" rel="stylesheet">
    </head>
    <body>
        {{> menu}}
        <div class="detailed-plant">
            {{#Plant}}
            <main class="plant-main">
                <div class="imageName">
                    <img src="{{imageURL}}" width=300>
                    <h2 class="details-plant-name">{{common_name}}</h2>
                </div>
            </main>
            <div class="descriptionBox">
                <p>Scientific name: {{scientific_name}}</p>
                <p>Watering schedule: {{watering_schedule}}</p>
                <p>Light requirement: {{light_requirement}}</p>
                <p>Soil type: {{soil_type}}</p>
                <a href="/account/add-post/{{id}}"><button>Add Post</button></a>
            </div>
            <aside class="show-posts">
                {{#Posts}}
                <ul>
                    <li>
                        <img src="{{imageURL}}" width="300">
                        <div class="details-post-content">
                            <a href="/account/post-details/{{id}}"><p>{{body}}</p></a>
                            <div class="details-post-buttons">
                                <a href="/account/edit-post/{{id}}"><button>Edit</button></a>
                                <form method="POST" action="/account/delete-post">
                                    <input type="hidden" name= "post_id" value= "{{id}}">
                                    <button>Delete</button>
                                </form>
                            </div>
                        </div>
                    </li>
                </ul>
                {{/Posts}}
            </aside>
            {{/Plant}}
        </div>
        <footer>
        {{> footer}}
        </footer>
    </body>
</html>















</html>