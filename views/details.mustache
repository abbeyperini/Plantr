<!DOCTYPE html>
<html lang='en'>
    <head>
        <meta charset='UTF-8'/>
        <meta name='viewport' content='width=device-width' initial-scale='1.0' maximum-scale='1.0'/>
        <title>Plantr</title>
        <link rel="shortcut icon" href="/images/plantrLogo.svg" type="image/x-icon">
        <link rel='stylesheet' href='/styles/styles.css'/>
        <link href="https://fonts.googleapis.com/css2?family=Comfortaa:wght@400;700&display=swap" rel="stylesheet">
    </head>
    <body>
        {{> menu}}
        {{#Plant}}
        <main class="posts-main">
            <div class="imageName">
                <img src="{{imageURL}}" width=300>
                <h2>{{common_name}}</h2>
            </div>
            <div class="descriptionBox">
                <p>{{scientific_name}}</p>
                <p>{{watering_schedule}}</p>
                <p>{{light_requirement}}</p>
                <p>{{soil_type}}</p>
                <a href="/account/add-post/{{id}}"><button>Add Post</button></a>
            </div>
        </main>
        <aside class="show-posts">
            {{#plant_post}}
            <ul>
                <li>
                    <img src="{{imageURL}}" width="300">
                    <h2>{{plant}}<h2> 
                    <p>{{body}}</p>
                    <a href="/account/edit-post/{{id}}"><button>Edit</button></a>
                    <form method="POST" action="/account/delete-post">
                        <input type="hidden" name= "post_id" value= "{{id}}">
                        <button>Delete</button>
                    </form>
                </li>
            </ul>
            {{/plant_post}}
        </aside>
        {{/Plant}}
        <footer>
        <!-- {{> footer}} -->
        </footer>
    </body>
</html>















</html>